# from itertools import permutations
# word , numberOfPerms = input(" enter word then number of permutations: \n").split()

# for i in list(permutations(sorted(word),int(numberOfPerms))): #given a word, and number of perms, find permutations, create a list, iterate over that list
#       print(*i,sep='')
